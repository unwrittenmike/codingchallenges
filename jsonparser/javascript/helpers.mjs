export const isLeftCurlyBrace = (caracter) => caracter === "{";

export const isRightCurlyBrace = (caracter) => caracter === "}";

export const isDoubleQuote = (caracter) => caracter === `"`;

export const isComma = (caracter) => caracter === ",";

export const isColon = (caracter) => caracter === ":";

export const isKey = () => true;

export const isValue = () => true;
